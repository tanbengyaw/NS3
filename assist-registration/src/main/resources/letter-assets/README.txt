Place legacy ASSIST letter images here for PDF rendering:

- national_emblem.png
- sst_kastam_logo.png

Sources (pick one):

1. Copy into this folder (`assist-registration/src/main/resources/letter-assets/`)
2. Set `registration.letter.assets-path` in `application-dev.properties`
3. Use legacy dev folder if present: `C:/development/pdf_template/images`

If no images are found, the letter still generates with transparent placeholders.
